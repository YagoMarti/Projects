@model IEnumerable<SistRepos.Models.reposiciones>

@{
    ViewBag.Title = "Sistema de Reposiciones";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<form role="form" action="@Url.Action("Index3")" method="Post" class="form-horizontal">

    <div class="form-group">
        <div class="col-lg-3"></div>
        <div class="col-lg-4">
            <fieldset>
                <legend>Solicitudes finalizadas por Solicitante</legend>
                <div class="form-group">
                    <label for="nombre" class="col-lg-4 control-label">Elija el Operario</label>
                    <div class="col-lg-8">
                        @Html.DropDownList("idSolicitante", null, String.Empty, new { @class = "form-control" })
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-lg-2 col-lg-offset-4"><input type="submit" value="Consultar" class="btn btn-default" /></div>
@if (Model != null)
{
                    <div class="col-lg-2 col-lg-offset-4">
                        <input type="button" class="btn btn-default" onclick="window.open('/JefeSolicitante/Index3Print?idSolicitante=' + @ViewBag.sol, 'newPage');" name="print" value="Imprimir" />
                    </div> }
                </div>
            </fieldset>
        </div>
    </div>
</form>

@if (Model != null)
{
    <table class="table table-striped table-hover">
        <tr>
            
            <th>
                @Html.DisplayNameFor(model => model.horaCreacion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.horaAsignacion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.horaResolucion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.herramientas.herramienta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.puestos.puesto)
            </th>
            <th>
                Operario
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.horaCreacion)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.horaAsignacion)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.horaResolucion)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.herramientas.herramienta)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.puestos.puesto)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.operarios.username)
                </td>
                <td>
                    @Html.ActionLink("Detalles", "Details", new { id = item.idReposicion })
                </td>
            </tr>
        }

    </table>
}